// Represents a Data Transfer Object (DTO) for user login details.
// This is not a database entity but a structure used for carrying login data.
export class Login {

    // The user's email address for authentication
    email: string;

    // The user's password for authentication
    password: string;
}